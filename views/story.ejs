<% include partials/header %>

   <main>
      <h1><%= heading %></h1>

      <h2>
         <%= story.title %>
      </h2>

      <a href="<%= story.url %>">
         <%= story.url %>
      </a><br>

      By <a href="/user/<%= story.by %>">
         <%= story.by %>
      </a><br>                  
      <%= story.score %> points

      <% if(comments && comments.length > 0) { %>
         <ul>
            <% comments.forEach(comment => { %>
               <li>
                  <%- comment.text %>
                  by <strong><%- comment.by %></strong>
               </li>
            <% }) %>   
         </ul>
      <% } %>

   </main>

<% include partials/footer %>   
